# 构建产物（由 npm run build 生成，install.mjs 会自动重新构建；仅根目录，
# 不影响 scripts/lib/ 共享源码）
/lib/

# 构建暂存目录（原子构建用：产物先写这里，全部成功后才整体换上 lib/；
# 正常退出会自行清理，进程被强杀时可能留残，属可丢弃目录）
/.build-stage/

# pnpm pack 产物
*.tgz

# 依赖与系统文件
node_modules/
.DS_Store
